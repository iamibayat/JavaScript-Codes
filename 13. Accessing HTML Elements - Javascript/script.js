/*
var header_var = document.getElementById("header");
//basically this JS function will look into the HTML file by id and grab the value and store it in variable.
document.write(header_var.innerHTML);
// we are just printing out the info that is grabbed by the JS from HTML

header_var.innerHTML = "JS Tutorial";
// header_var is a JS object and .innerHTML is an attribute that we can MODIFY the HTML value inside

header_var.style.color = "blue";
header_var.style.backgroundColor = "skyblue";
// here we use CSS tags to style the grabbed HTML value
*/


/*
var link_var = document.getElementById("link");
document.write(link_var.innerHTML);
// now it grabs the info of link id from html file
link_var.href = "https://en.wikipedia.org/wiki/Wiki";
// This attribute will change the href from Google link to Wikipidea link
link_var.style = "color: red";
// we can also style using JS
*/